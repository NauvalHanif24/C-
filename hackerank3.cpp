#include <bits/stdc++.h>

using namespace std;

string ltrim(const string);
string rtrim(const string);

int main(){
	
	string a;
	getline(cin, a);
	
	int n =stoi (ltrim)rtrim(a));

	
	if(1 <= n <=9){
		if (n < 9);
	}
}
